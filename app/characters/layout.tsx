import React from "react";

export default function CharactersLayout({children}:{ children: React.ReactNode,}){
   return(
      <section>
         Char -- here
         {children}
      </section>
   )
}
